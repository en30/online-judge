#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, N) for (int i = 0; i < (int)N; i++)

int N;

int main () {
  cin >> N;
  cout << (N >= 1000 ? "ABD" : "ABC") << endl;

  return 0;
}
