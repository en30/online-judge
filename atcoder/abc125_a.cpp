#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, N) for (int i = 0; i < (int)N; i++)

int main () {
  int a,b,t;
  cin >> a >> b >> t;

  cout << b * (t / a) << endl;

  return 0;
}
