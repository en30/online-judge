#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, N) for (int i = 0; i < (int)N; i++)

int main () {
  int x;
  cin >> x;
  cout << ((x == 3 || x == 5 || x == 7) ? "YES" : "NO" ) << endl;
  return 0;
}
